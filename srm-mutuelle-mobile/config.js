// Backend Spring : alignez le port avec SERVER_PORT dans srm-mutuelle-backend/.env
// (8082 si 8081 est pris — souvent par Expo / Metro sur le même PC).
// Remplacez l’hôte par l’IPv4 du PC (ipconfig), même Wi-Fi que le téléphone.
export const API_BASE_URL = 'http://192.168.114.67:8082';
